jQuery(document).ready(function() {
    /* ---------------------------------------------- /*
     * ZoomIn
    /* ---------------------------------------------- */
    const zoomFrame = $('.item-img');

    zoomFrame.mousemove(cllbackFn);
});